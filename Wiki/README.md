# Wiki

This directory is the repository knowledge center.

Rules:
- Keep the wiki synchronized with repository changes.
- Prefer concise, factual pages that map to one canonical source.
- Update `index.md` when pages are added or their scope changes.
- Append new events to `log.md`; do not rewrite history.

Current state:
- The repository contains a working EPA 2024 CDR transformation, workbook, spatial, and dashboard pipeline.
- Root governance files, the Python virtual environment, source data, generated artifacts, and git metadata are present.
- `README.md` is the public project overview; this wiki contains the detailed script, schema, output, and history pages.
